; Project-local override: the global npmrc sets omit=["dev"],
; which silently skips devDependencies (typescript, eslint,
; @tailwindcss/postcss, tailwindcss). This project needs them
; to build, so re-enable dev deps here.
include=dev