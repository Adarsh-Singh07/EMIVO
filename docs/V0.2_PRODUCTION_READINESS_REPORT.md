# ELEKTRIX v0.2 — Production Readiness Report

**Date:** 2026-08-17
**Target Environment:** Oracle VPS (api, admin, workers, redis) & Vercel (storefront, admin fallback)
**Commit SHA:** `3df3d9891da11540575eda3dc6f26e6fdcb2488d`

## 1. Issues Found & Severity

| ID | Issue | Severity | Description |
|---|---|---|---|
| C1 | CI/CD Pipeline Broken | CRITICAL | GitHub Actions referenced non-existent `apps/web` for admin builds. |
| C2 | Stale Admin Container | CRITICAL | Admin VPS container was running deprecated `ghcr.io/elektrix/web:latest` image. Dockerfile build failed due to Turbopack vs `@tailwindcss/postcss` Alpine incompatibility. |
| C3 | Missing Image CDN Domains | CRITICAL | Storefront `next/image` config lacked `media.elektrix.in` (R2) and `picsum.photos` (seeded images). |
| C4 | Unsafe Diagnostics Endpoint | CRITICAL | `/health/diagnostics` exposed internal VPS details without authentication. |
| H1 | Canonical URL Mismatch | HIGH | Product route `[id]` folder conflicted with canonical `<link>` and LD-JSON using `[slug]`. |
| H2 | Production Source Maps Exposed | HIGH | Storefront and Admin allowed Next.js browser source maps in production, exposing source code. |
| M1 | Over-permissive CORS | MEDIUM | `sell.elektrix.in` was in the allowed origins but seller marketplace is not in v0.2 scope. |

---

## 2. Fixes Applied

*   **Fixed CI Pipeline:** Updated `.github/workflows/ci.yml` to correctly point to `admin/` instead of `apps/web`. The workflow now correctly builds and type-checks `admin` and `storefront` from their respective directories.
*   **Fixed Admin Docker Build:** Modified `admin/Dockerfile` to copy `package-lock.json` and use `npm ci` for reproducible builds. Pinned `next` to `16.2.12` in `admin/package.json` to resolve an Alpine Turbopack/PostCSS bug, successfully completing the Docker build.
*   **Secured Next.js:** Disabled `productionBrowserSourceMaps` in both `storefront/next.config.ts` and `admin/next.config.js`. Added `media.elektrix.in` and `picsum.photos` to the storefront remote image patterns.
*   **Secured Diagnostics Endpoint:** Updated `apps/api/main.py` to enforce `require_staff` authentication on the `/health/diagnostics` and `/api/v1/system/status` endpoints.
*   **Fixed Product Routing:** Renamed `storefront/app/product/[id]` to `[slug]` and updated the Next.js page props to destruct `{ slug }` instead of `{ id }`, perfectly matching the canonical SEO tags and API identifiers.
*   **Hardened CORS:** Removed `sell.elektrix.in` from `.env` `CORS_ORIGINS` to reduce the attack surface.

---

## 3. Tests Performed & Production URLs Tested

*   ✅ `curl -sk -I "https://localhost/" -H "Host: admin.elektrix.in"`: Admin Next.js app serving HTTP 200 via Nginx.
*   ✅ `curl -sk -I "https://localhost/" -H "Host: elektrix.in"`: Storefront app serving HTTP 200 via Nginx.
*   ✅ `docker compose build admin`: Verified standalone admin Next.js Alpine Docker build works natively without Turbopack postCSS panic.
*   ✅ Fastapi API restart: Verified `elektrix-api-1` and `elektrix-api-2` correctly pick up the `require_staff` restriction and modified `.env`.

**Production URLs Verified (Nginx Proxy):**
*   `elektrix.in` → Storefront Container (Port 3000)
*   `admin.elektrix.in` → Admin Container (Port 3100)
*   `api.elektrix.in` → FastAPI API Container (Port 8000)

---

## 4. Docker Containers Status

All required containers are natively rebuilt, healthy, and running on the VPS:

| Name | Image | Status | Ports |
|---|---|---|---|
| `elektrix-api-1`, `2` | `ghcr.io/elektrix/api:latest` | **Up (healthy)** | 8000/tcp |
| `elektrix-admin-1`, `2` | `ghcr.io/elektrix/admin:latest` | **Up (healthy)** | 3100/tcp |
| `elektrix-storefront-1`, `2` | `ghcr.io/elektrix/storefront:latest` | **Up (healthy)** | 3000/tcp |
| `elektrix-workers-1` | `ghcr.io/elektrix/workers:latest` | **Up** | - |
| `elektrix-redis-1` | `redis:7-alpine` | **Up (healthy)** | 6379/tcp |
| `elektrix-nginx-1` | `nginx:alpine` | **Up** | 80/tcp, 443/tcp |

---

## 5. Remaining Blockers & Manual Actions Required

No technical blockers remain for v0.2. The VPS architecture strictly matches your requested target architecture (Storefront and Admin completely separate Next.js builds proxying to the single FastAPI).

**Exact Manual Actions You Need to Perform:**

1.  **Push to GitHub:** My changes have been committed locally. Run `git push origin main` to push the fixes and trigger the (now fixed) GitHub Actions CI/CD pipeline.
2.  **Verify DB Credentials:** The database is using Supabase connection pooler strings. You asked me not to rotate credentials destructively. Please log into the Supabase dashboard and rotate the PostgreSQL password manually to ensure the old leaked credentials are invalidated. Update the `.env` on the VPS once done.
3.  **End-to-End Test:** After GitHub Actions deploys the code, perform a manual end-to-end checkout flow (add product to cart -> enter shipping -> mock Razorpay payment) to verify complete system integration.
